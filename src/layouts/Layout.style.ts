import styled from 'styled-components';

export const LayoutView = styled.div`
  /* max-width: 36rem; */
  margin: 0 auto;
`;

export const OutletPart = styled.div`
  display: flex;
  flex-direction: column;
  flex: 1;
  min-height: calc(100vh - 6.4rem);
`;
